-- Executado automaticamente pelo container do Postgres na primeira inicialização.
-- Cria um banco de dados separado para a Evolution API (v2 exige um banco configurado,
-- mesmo quando você não usa a persistência dela para nada além do controle interno).
CREATE DATABASE evolution;
